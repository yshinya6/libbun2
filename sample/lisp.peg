
TopLevel    <- Expr;
Expr    <- '(' _? {($Expr)? (_ $Expr)* } ')' / Integer / Symbol;
Integer <- { [0-9]+ #integer };
Symbol  <- { ([a-zA-Z]+ #symbol) / ('+'  #add) /  ('-' #sub) /  ('*' #mul) / ('/' #div) / ('%' #mod) / ('<' #lt) / ('>' #gt) };
_       <- [ \t\n];
