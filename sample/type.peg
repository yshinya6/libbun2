
_          <- ([ \t]+)* ;

Type        <- {[A-Za-z] [A-Za-z0-9_]*   #type};
Type        <- Type {$ '[]'             #atype} / Type;
Type        <- Type {$ '<' _ $Type (_ ',' _ $Type)? _ '>' #ptype} / Type;
Name        <- {[A-Za-z]+ #name};

TopLevel    <- Type;


